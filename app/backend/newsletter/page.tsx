import { NewsletterClient } from "./_client";

export const metadata = {
  title: "Kelola Newsletter | Admin",
  description: "Kelola Newsletter dari pengguna website",
};

export default function NewsletterPage() {
  return <NewsletterClient />;
}
